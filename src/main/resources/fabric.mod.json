{
  "schemaVersion": 1,
  "id": "create_computers",
  "version": "${version}",
  "name": "Create Computers",
  "description": "Expand the Create mod with python-compatible computers!",
  "authors": [
    "Merkli"
  ],
  "contact": {
  },
  "license": "MIT License",
  "icon": "assets/create_computers/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "me.merkli.create_computers.CreateComputersMod"
    ]
  },
  "mixins": [
    "create_computers.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "create": "${create_version}",
    "minecraft": "${minecraft_version}"
  }
}
